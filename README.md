
# BCWX Landing (GitHub Pages)

A tiny, neon-forward landing page for the BabyCatWifX meme token.

## Deploy
1. Create a new GitHub repo (e.g., `bcwx-site`).
2. Upload everything in this folder to the repo root.
3. In **Settings → Pages**, set Source to `main` branch `/ (root)`.
4. Site will publish at `https://<username>.github.io/<repo>/`.

## Replace
- In `index.html`, replace `BCWX_ADDRESS` with your Raydium outputCurrency address.
- Update X/Telegram/Discord URLs in the top nav.
